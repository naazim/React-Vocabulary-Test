# React-Vocabulary-Test
A React app to add words in a native language, add the corresponding translation, and test the skills accordingly.

## Installation
```
yarn install
yarn run build:prod
yarn run serve
```

